import type { Metadata } from "next";

export const metadata: Metadata = {
    title: "Create Event | Event-AI",
    description: "Plan and create your next event with Event-AI. Set up event details, budget, and find the perfect vendors.",
};

export default function CreateEventLayout({ children }: { children: React.ReactNode }) {
    return children;
}
